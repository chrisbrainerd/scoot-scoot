{
  "name": "scoot-scoot",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "build": "echo 'PRESTART' && cd scoot-scoot-ui && npm ci && GENERATE_SOURCEMAP=false npm run build && echo 'DONE'",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "leaflet": "^1.5.1",
    "node-fetch": "^2.6.1",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  },
  "description": ""
}
